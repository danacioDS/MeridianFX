[project]
name = "meridian-fx"
version = "0.1.0"
description = "Meridian FX — Layer 2 Decision Engine"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"